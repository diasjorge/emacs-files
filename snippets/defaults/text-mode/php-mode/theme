# -*- mode: snippet -*-
# contributor : Da Costa Alcindo <sir.squall.leonheart@gmail.com>
# name: drupal
# key: theme
# --

/**
 * Implementation of hook_theme().
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_theme($existing, $type, $theme, $path){
  return array(
    '$2' => array(
      'arguments' => array('$2' => '$3'),
    ),
  );
}
$0
